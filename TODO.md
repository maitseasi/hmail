# Hmail – Feature TODO

## Hard

### Thread action bar
- **Reply Together** – compose a single reply to multiple threads (batch compose view)
- **Collection** – group threads into a named collection / bundle
- **Merge** – merge two or more threads into one

### Per-message menu
- **Report spam** – backend needs `gmail.modify` to move to SPAM

### Workflow views
- **Power Through "everything" / "all done" state** – celebratory empty-state screen when no messages remain
- **Screener bulk accept** – accept all screener messages at once with confirmation

### Bulk actions
- **Reply Together** – compose a single reply touching every selected thread
- **Collection** – add selected threads to a named collection
- **Merge** – merge selected threads into one

### Imbox
- **Clips** – short audio/video notes attached to threads (record, playback, transcript)
